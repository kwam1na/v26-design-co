import GenericPage from "../components/generic-page";

export default function NotFoundPage() {
  return (
    <GenericPage
      title="404 | v26 Design Co."
      headerText="404."
      body="Hmmm, looks like that page that doesn't exist."
    />
  );
}
